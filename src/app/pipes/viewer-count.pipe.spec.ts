import { ViewerCountPipe } from './viewer-count.pipe';

describe('ViewerCountPipe', () => {
  it('create an instance', () => {
    const pipe = new ViewerCountPipe();
    expect(pipe).toBeTruthy();
  });
});
