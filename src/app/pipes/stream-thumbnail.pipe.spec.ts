import { StreamThumbnailPipe } from './stream-thumbnail.pipe';

describe('StreamThumbnailPipe', () => {
  it('create an instance', () => {
    const pipe = new StreamThumbnailPipe();
    expect(pipe).toBeTruthy();
  });
});
